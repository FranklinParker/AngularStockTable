import { CapitializeFirstLeterPipe } from './capitialize-first-leter.pipe';

describe('CapitializeFirstLeterPipe', () => {
  it('create an instance', () => {
    const pipe = new CapitializeFirstLeterPipe();
    expect(pipe).toBeTruthy();
  });
});
